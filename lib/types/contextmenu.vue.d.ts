import { Vue } from "vue-property-decorator";
export default class ContextmenuType extends Vue {
    private cancelMouseup?;
    private cancelMousedown?;
    private menu();
    contextmenu(mousedown: MouseEvent): void;
    private close();
    private clearCancellers();
}
